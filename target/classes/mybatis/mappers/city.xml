<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="city">
    
    <!-- 도시 추가하기 -->
	<insert id="insert" parameterType="cityvo">
		<![CDATA[
			insert 
			  into city 
			values (null, '바르셀로나', now(), 10)
		]]>
	</insert>

	<!-- 도시 삭제하기 -->
	<delete id="delete" parameterType="cityvo">
		<![CDATA[
			delete 
			  	from city
			 where cty_name = '바르셀로나'
		]]>
	</delete>

	<!-- 도시 수정하기 -->
	<update id="update" parameterType="cityvo">
		<![CDATA[
			update city 
				SET cty_name = '바르셀로나', ctr_seq = 10
			WHERE CTY_seq = 15
		]]>
	</update>

	<!-- 도시 검색하기 -->
	<select id="getCityList" parameterType="String" resultType="cityvo">
	    <![CDATA[
			select c1.cty_seq, c1.cty_name, c1.crt_date, c1.ctr_seq, c.ctr_name
				from country c, city c1
			where c1.CTR_SEQ = c.CTR_SEQ
		]]>
	</select>

</mapper>