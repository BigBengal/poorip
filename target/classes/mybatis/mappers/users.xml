<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<!-- 새로운 유저 입력 -->
	<insert id="insert" parameterType="uservo">
		<![CDATA[
			insert 
			  into users 
			values (#{usrEmail },'자기소개','M','KOR','20000101','닉네임','간략소개','해쉬태크','1','1','5','3','2','N',now(), now(),'N')
		]]>
	</insert>

	<!-- 사용자 로그인 정보 업데이트 -->
	<update id="updatelogin" parameterType="uservo">
	<![CDATA[
		update users set USR_LOGIN_DATE = now() where usr_email = #{usrEmail}
	]]>
	</update>

	<!-- 사용자 자기소개/해쉬태그 정보 업데이트 -->
	<update id="updateprofile" parameterType="uservo">
	<![CDATA[
		update users set usr_info = #{usrInfo}, usr_hashtag = #{usrHashtag} where usr_email=#{usrEmail}
	]]>
	</update>

	<!-- 프로필 수정에 표시할 내용 불러오기 -->
	<select id="selectbyemail" resultType="uservo" parameterType="uservo">
		<![CDATA[
			select usr_info as usrInfo, usr_hashtag as usrHashtag from users where usr_email = #{usrEmail}
		]]>
	</select>

	<!-- 노티피케이션 수정 -->
	<update id="updatenoti" parameterType="uservo">
		<![CDATA[
			update users set usr_noti = #{usrNoti} where usr_email=#{usrEmail}
		]]>
	</update>

	<!-- 블랙리스트 추가 -->
	<update id="updateblack" parameterType="uservo">
		<![CDATA[
			update users set usr_black_yn = #{usrBlackYN} where usr_email=#{usrEmail}
		]]>
	</update>

	<!-- 사용자 삭제 -->
	<delete id="delete" parameterType="uservo">
		<![CDATA[
			delete 
			  from users
			 where usr_email = #{usrEmail }
		]]>
	</delete>

</mapper>